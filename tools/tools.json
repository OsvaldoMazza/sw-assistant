[
    {
        "type": "function",
        "function": {
            "name": "get_current_weather",
            "description": "Get the current weather",
            "parameters": {
                "type": "object",
                "properties": {
                    "location": {
                        "type": "string",
                        "description": "The city and state, e.g. San Francisco, CA"
                    },
                    "format": {
                        "type": "string",
                        "enum": ["celsius", "fahrenheit"],
                        "description": "The temperature unit to use. Infer this from the users location."
                    }
                },
                "required": ["location", "format"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "get_n_day_weather_forecast",
            "description": "Get an N-day weather forecast",
            "parameters": {
                "type": "object",
                "properties": {
                    "location": {
                        "type": "string",
                        "description": "The city and state, e.g. San Francisco, CA"
                    },
                    "format": {
                        "type": "string",
                        "enum": ["celsius", "fahrenheit"],
                        "description": "The temperature unit to use. Infer this from the users location."
                    },
                    "num_days": {
                        "type": "integer",
                        "description": "The number of days to forecast"
                    }
                },
                "required": ["location", "format", "num_days"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "get_calendar_events",
            "description": "Get the events of calendar. Don't say de id event",
            "parameters": {
                "type": "object",
                "properties": {
                    "number_events": {
                        "type": "integer",
                        "description": "the number of upcoming events. e.g. 5"
                    },
                    "days": {
                        "type": "integer",
                        "description": "The days when start to get the events of calendar. e.g. 7"
                    }
                },
                "required": []
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "set_calendar_event",
            "description": "Set the events of calendar, or update event.If is an update event, call first the 'get_calendar_events' function to retrieve the 20 next events and find the event to update, then call this function again with the eventIs, start_dateTime, end_dateTime and other existing propierties from the event, and the new values to update",
            "parameters": {
                "type": "object",
                "properties": {
                    "summary": {
                        "type": "string",
                        "description": "the title or summary of the event. e.g. 'dentist'"
                    },
                    "location": {
                        "type": "string",
                        "description": "The place of the event. e.g. Caballito"
                    },
                    "description": {
                        "type": "string",
                        "description": "More details of the event"
                    },
                    "start_datetime": {
                        "type": "string",
                        "description": "The start datetime of the event in UTC format, e.g. 2024-09-30T09:00:00"
                    },
                    "end_datetime": {
                        "type": "string",
                        "description": "The end datetime of the event in UTC format. If it None, then set 1 hour after the start datetime, e.g. 2024-09-3010:00:00"
                    },
                    "reminder_minutes": {
                        "type": "integer",
                        "description": "A reminder of the event. By default set 60, e.g. 60"
                    },
                    "eventId": {
                        "type": "string",
                        "description": "ID of event to update. Use only to update an existing event."
                    }
                },
                "required": ["summary", "start_datetime", "end_datetime"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "play_youtube",
            "description": "play a song or video in youtube. Create a short query with information",
            "parameters": {
                "type": "object",
                "properties": {
                    "title": {
                        "type": "string",
                        "description": "The name of son with the author, or the video to play. Example: 'purple rain , Prince'. Example: 'highlights match soccer world cup 2022'"
                    }
                },
                "required": ["title"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "stop_youtube",
            "description": "stop a song or video in youtube.",
            "parameters": {
                "type": "object",
                "properties": {
                },
                "required": []
            }
        }
    }
]